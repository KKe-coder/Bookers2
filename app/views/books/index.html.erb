<% @books.each do |book| %>
<div>
  <p>投稿ユーザー画像:<%= attachment_image_tag book.user, :profile_image, :fill, 60, 60, format:'jpeg', fallback: "no_image.jpg" %></p>
  <p>Title:<%= book.title %></p>
  <p>Opinion:<%= book.body %></p>
</div>
<% end %>