<% @user.each do |user| %>
<div>
  <p>User image:<%= attachment_image_tag user, :profile_image, :fill, 60, 60, format:'jpeg', fallback: "no_image.jpg" %></p>
  <p>User name:<%= user.name %></p>
  <p><%= link_to "Show", user_path(user.id) %></p>
</div>
<% end %>